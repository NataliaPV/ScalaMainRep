New.

- set main class in tag:

<manifest>
      <mainClass>package_name.class_name</mainClass>
</manifest>

- execute compilation and packaging

package


Legacy. How to build executable (e.g. "fat" jar)

- set main class in tag:

<manifest>
      <mainClass>package_name.class_name</mainClass>
</manifest>

- execute compilation and packaging

mvn clean compile assembly:single